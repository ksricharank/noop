# Fork release 10.6.0.15.4 (build 294)

Three maintainer asks from the 260901 evening session (all `feature/v15-widgets`; the detector
knobs also touch `StrandAnalytics`).

## Target derivations, plain-language pass (third iteration)

Every line of "How these were set" now refers to a number and reads plainly to a new person:
four blocks (the session ladder lives inside Effort), "readiness" unpacked into the **body
check** — the signals' actual values against their 30-day baselines ("HRV 62ms ≈ your usual
60ms, resting HR 55 ≈ usual 56 → all normal") — the workout pace derived in words from resting
HR toward max, the 0–100 effort scale defined before it's used, untaken branches shown inline,
zero-adjustment rungs still printed, and no notch/zone/TRIMP/Karvonen/Keytel/RMR/clamp anywhere
(pinned by a jargon sweep in TargetsExplainerTests).

## Breathe sensitivity is configurable (target ~5 nudges/day)

The stress check-in card gains two knobs: **Dip depth** (fires when HRV drops below N% of
baseline; ships 60%, range 50–80) and **Dip must last** (ships 60 s, range 15–120). Raising dip
depth toward 70–75% is the lever for more frequent nudges. Calibration evidence: a new day-keyed
export line — `Breathe cues today: fired=N (live=L retro=R) last=HH:mm:ss` — so the next log says
what a setting actually yields. Engine defaults unchanged; existing detector vectors byte-identical.

## Target automations (A / B / E), all default-off toggles in Automations

- **Morning brief (A):** the day's plan as ONE notification the moment the first post-wake sync
  scores the night — "Charge 80 · 45 min workout @ ~139 bpm · 10000 steps · sleep 8h21 tonight" —
  held until a configurable earliest time (default 06:00). Fires before the app is ever opened
  (rides the background post-offload path).
- **Wind-down (B):** the EXISTING wind-down reminder (Alarms screen) gains "Use tonight's sleep
  target" — the reminder time then moves with the computed target (charge/Rest/body check/ledger)
  instead of assuming a fixed 8 h night. Re-applied after every fresh score; reschedules only on
  change.
- **Target pacing (E, rewired same evening):** intelligent check-ins, not presets: at the top of
  every N hours (configurable, default 2) through the waking window (8:00–22:00), the day so far
  is compared with the TRUE prorated pace — no fixed slack percentage — and only a genuine
  shortfall nudges, sized by the deficit ("1500 steps — 2785 behind the 4285 you'd be at on pace
  for 10000; ~27 min of walking catches you up"). An undone workout rides along as context and
  never triggers alone; with no step target, effort prorates as the primary check. At most one
  nudge per checkpoint; on-pace check-ins are consumed silently; a sleep-through evaluates only
  the latest due checkpoint. Existing automations (illness watch, inactivity reminder) untouched.

Pinned: TargetAutomationsTests (brief once-per-day matrix + copy; pacing behind/on-pace/second-
checkpoint/rest-day), BreatheCueStatsTests, two new StressOnsetDetector Config vectors.

## Verify on phone

- About shows 10.6.0.15.4 (294). Today → "How these were set" reads plainly with today's numbers.
- Automations shows Morning brief + Target pacing cards; stress card shows the two new knobs.
- Alarms → wind-down shows "Use tonight's sleep target".
- Next log: `Breathe cues today:` line present once a nudge fires; automation posts logged as
  "Automation: morning brief posted (…)" / "Automation: pace check posted (…)".
