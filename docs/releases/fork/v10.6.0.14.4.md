# Fork build 10.6.0.14.4

> **PARKED — never installed.** The user chose to stay on `10.6.0.14.2` after its freeze cleared
> with an app restart (twice), and to build forward from `.14.2`. These commits live on
> `feature/live-activity-freeze-forensics`, NOT on `feature/locked-stream-duty-cycle`. If the
> `.14.2` freeze recurs: build and install this, reproduce, toggle the activity off/on, export —
> the push-ledger line convicts either the system (counting → the entitlement here is the cure) or
> the `.14.2` controller changes (stalled → revert those). If `.14.2` stays stable, delete the
> branch and these notes.

**Build:** 268 · **Date:** 28 August 2026 · **Platform:** iOS (sideload) · Base upstream `v10.6.0`.

Adds the **push ledger** on top of `.14.3` (never installed separately). The frozen-card
investigation hit an attribution wall: the 260828-1625 log shows ticks flowing and zero errors —
but cannot distinguish "the controller went silent" (a bug in the `.14.2` changes; the user's
+1-was-perfect history points here) from "the system discarded the updates" (the missing
frequent-updates entitlement `.14.3` declares). Every activity now counts the pushes actually
handed to ActivityKit (live/locked), and each new start logs the PREVIOUS card's totals:

> `Live Activity: started — frequent updates enabled; previous card received live=812 locked=3 pushes`

**The verdict rule:** reproduce a freeze, toggle the Live Activity off/on, export.
A card that visibly froze while its ledger kept counting **convicts the system** (ration theory
confirmed; the `.14.3` entitlement is the fix). A stalled ledger **convicts the controller** — the
`.14.2` changes get reverted immediately (the `.13` corpse fixes and the entitlement stay).

Verified: NOOPiOS compiled; frequent-updates key present in both built Info.plists.
