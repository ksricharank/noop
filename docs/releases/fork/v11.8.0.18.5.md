# 11.8.0.18.5 (build 406)

Type only: the Targets widget's values drop 17 → 15pt, against their 13pt labels — "just a tad bit
larger than the font of the words". The bold weight carries the emphasis instead of the size, so
the pair still reads as the value and the label as its name.

## Still open for the next log

Unchanged since 18.0:

1. **`writeBack=<each>×<runs>`** — the first honest read of the Apple Health write cost. Compare
   the per-run figure to the reads on the same line, never to an earlier log's `writeBack`, which
   was divided by the wrong denominator.
2. **Whether `maxMs` and `longest` have stopped being the same integer.** They shared a key until
   18.0.
3. Whether the Today summary reads as a state rather than a target list — 18.1 added the missing
   six-hour context, so the log is what says whether the absent input was the whole story.
