# Fork release 10.6.0.15.3 (build 293)

One maintainer iteration on 15.2's "How these were set" section: the derivations now read as
CODE, not narrative (`feature/v15-widgets`). Each target is a monospaced block of
`input comparison → consequence` rungs — the taken branch of the real if/else with its real
threshold (`charge 80 ≥ 67 → base 45m (push)`), the session notch re-walked from the inputs
(never inferred back from the minutes), every branch evaluated rather than omitted (a balanced
ledger still prints `debt 0m ≤ 20m → +0m`), and every block ending in the exact number the strip
displays. Five blocks: SESSION (the shared notch ladder + Karvonen line), EFFORT (trimp product →
strain curve), CAL (resting + Keytel session → rounded sum), STEPS (band base, trim, clamp),
SLEEP (population base, four adjustment rungs, debt min(), clamp). Placement unchanged — the
collapsed disclosure between the four numbers and the LLM narrative.

Pinned by the rewritten TargetsExplainerTests (branch text against the published constants; the
stated arithmetic must reproduce the displayed target).

## Verify on phone

- About shows 10.6.0.15.3 (293).
- Today → "How these were set": five monospaced blocks, each rung showing today's real values
  against the real thresholds, each block's headline number matching the strip above it.
